# Core framework
agentarium>=0.3.1
aisuite>=0.1.7

# LLM / Ollama
ollama>=0.3.0
openai>=1.57.2

# Async + concurrency
asyncio-throttle>=1.0.2
aiofiles>=23.2.1
aiohttp>=3.9.0

# System monitoring
psutil>=6.0.0
watchdog>=4.0.0
inotify-simple>=1.3.5

# Data + storage
pydantic>=2.6.0
sqlalchemy>=2.0.0
alembic>=1.13.0

# CLI + display
typer>=0.12.0
rich>=13.7.0
prompt-toolkit>=3.0.43

# Research + web
requests>=2.31.0
duckduckgo-search>=6.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Serialization
pyyaml>=6.0.1
dill>=0.3.8
orjson>=3.9.0

# Security tooling
paramiko>=3.4.0
cryptography>=42.0.0
python-nmap>=0.7.1

# Fine-tuning (optional, local)
# unsloth>=2024.3  -- install separately if GPU present

# Fine-tuning (cloud providers, optional)
# together>=1.2.0  -- install if Together.ai key present
# replicate>=0.25.0

# Scheduling
apscheduler>=3.10.0

# Checksums + forensics
hashlib-additional>=0.1.0
python-magic>=0.4.27

# Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0
